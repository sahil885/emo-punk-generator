Re-target and consolidate blog posts on page-level data

Pulled per-URL GSC data before touching anything, which corrected the plan in
two places.

CONTENT-PLAN.md said re-target how-to-write-emo-lyrics to `punk lyrics
generator`. It's the best-performing page on the site — 405 impressions at
position 7.2 — so that would have risked the largest single source of
impressions we have. Title and meta rewritten for CTR instead (0.5% at
position 7.2 is the biggest remaining gap); target left alone.

The plan also marked song-for-your-best-friend for consolidation. It's the
fifth-best page by impressions, one week old, with a real click. Keyword
Planner reported no data for that term and GSC disagrees — same lesson as the
turn-texts family. Kept.

Re-targeted emo-breakup-song to `song about my ex` (100-1k, Low competition,
+900% three-month growth) against its previous target, which had no measurable
volume. Body now opens with the instruction rather than an essay.

ai-diss-track-generator sits at position 90.8 on a 100-1k keyword — an on-page
problem, not a merge candidate. It never actually said "AI diss track
generator" prominently while competing against rap-focused SERPs. Title and
opening fixed.

Merged the three genuinely dead pages (each under 6 impressions in 3 months)
via 301s: emo-song-ideas into the turn-your-words post, make-a-song-about-
someone into the gift page, how-to-make-a-sad-song-with-ai into the hub.
Source entries stay in lib/blog.ts flagged `mergedInto` rather than deleted,
and a new `livePosts` export drops them from the index, sitemap, and sibling
links — so reversing a merge is a one-line change. Two inbound links updated
so nothing points at a redirect.

what-makes-a-song-emo was already retitled in 584f32e and is left alone;
changing it twice in a week would make the CTR measurement unreadable.
